services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lineage_backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: lineage_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
